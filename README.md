# folder-tree

拖拽文件夹自动上传并生成文件树的思源笔记插件。

## 功能

- 拖拽文件夹到编辑器，自动上传所有文件
- 生成 Markdown 树形列表，保留文件夹结构
- 文件显示为可点击链接，文件夹显示 📁 图标
- 批量上传，每批 10 个文件

## 使用

1. 将文件夹拖入思源笔记编辑器
2. 等待上传完成
3. 自动生成文件树

## 开发

```bash
# 安装依赖
pnpm i

# 创建符号链接
pnpm run make-link

# 开发模式
pnpm run dev

# 构建
pnpm run build
```
